---
"hide-email-ext": patch
---

Suppress Firefox `data_collection_permissions` build warning in WXT config. Update `changesets/action` to v1.8.0 to fix deprecated `set-output` GitHub Actions warnings. Bump `actions/checkout` to v6 and `actions/cache` to v5.
